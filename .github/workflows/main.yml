name: Streamlit CI/CD Validation

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  test-and-lint:
    runs-on: ubuntu-latest

    steps:
    - name: Check out repository
      uses: actions/checkout@v3

    - name: Set up Python 3.9
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Test Streamlit Import
      run: |
        python -c "import streamlit"
        
  # ✅ 下方為部屬範例 (若您使用如 Heroku 或 Google Cloud Run)
  # 若使用 Streamlit Community Cloud 則完全不需要此流程，只要在官網綁定 GitHub Repo 即可自動部屬。
  # deploy:
  #   needs: test-and-lint
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - name: Deploy to Production
  #       run: echo "在這裡安插您的部屬指令 (如 git push heroku main)"
